// Database schema is now managed by Prisma.
// Run: npx prisma db push   (to apply schema to DB)
// Run: npm run seed         (to seed initial data)
console.log('Use "npx prisma db push" to initialise the database schema.');
process.exit(0);
